# Release notes 0.3.16

Estado: `OFFICIAL_DATA_WAVE_1_COMPLETE_ROADMAP_IN_PROGRESS`

## Nesta fatia

- Observabilidade estruturada de ingestão oficial (`ingest_started` / `ingest_finished` com `run_id`, volumes e falhas) na API e no job CLI.
- Dashboards oficiais consomem `emptySources`, homologação, competência, fórmula, qualidade e lineage por item.
- Testes parametrizados dos 15 dashboards e cobertura Angular de empty/lineage.
- Fonte regulatória `PLANALTO-LC-214` (LC 214/2025) com conector `official_document`, snapshot de CI e Gold `NON_BINDING`.
- Fonte `PORTAL-TRANSPARENCIA-TRANSFERENCIAS` catalogada como `PUBLIC_CONTROLLED` / `CREDENTIAL_REQUIRED` (sem scrape; ingest bloqueado).

## Não concluído

Backfill nacional, fontes estaduais estruturadas, território RFB, dados municipais reais, piloto, produção e homologação humana.
