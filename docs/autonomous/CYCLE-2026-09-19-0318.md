# Ciclo autônomo 0.3.18

Data: 2026-09-19
Branch: feat/official-public-ingest
PR: #2
HEAD pré-ciclo: `cdb3362`

## Seleção

Lote F5 relacionado: **F5-FINDINGS-CASES** + **F5-HUMAN-VALIDATION** (shells técnicos vazios).

## Entregas

- `GET /v1/findings` — página vazia; `createsTaxCredit=false`; `legalCommandsEnabled=false`; G5 BLOCKED.
- `GET /v1/cases` + `POST /v1/cases` — lista vazia; POST sempre 409 sem persistência.
- `GET /v1/human-validation` — workflow vazio; `publishesPublicCredit=false`; estados de referência documentais.
- Telas `/achados`, `/casos-auditoria`, `/validacao-humana` com loading/empty/erro.
- Migration aditiva `0021_findings_human_validation` (meta 0.3.18).
- Hardening do orquestrador (`run-roadmap-autonomous.ps1`: wait em fatias + CI poll).

## Evidência

- `evidence/releases/0.3.18/`
- Pytest direcionado: 17 passed (findings/cases/human-validation/health/alembic).
- Angular direcionado: 7 SUCCESS.

## Não feito neste ciclo

Backfills nacionais, fontes estaduais, RFB territorial, F5-NOTIFICATIONS/F5-COLLECTION, piloto, produção, homologação humana.
