# Ciclo autônomo — F5 notifications + collection (+ CI fix)

Data: 2026-09-19  
Branch: `feat/official-public-ingest`  
PR: #2  
HEAD pré-ciclo: `8f762c0`

## Seleção

- Corrigir CI Python vermelha (imports sem módulos).
- Completar `F5-NOTIFICATIONS` e `F5-COLLECTION` (shells vazios).

## Entrega

- Backend: domain/application/entrypoints para `/v1/notifications` e `/v1/collection`.
- Frontend: `/notificacoes` e painel oficial `/cobranca`.
- Testes unitários, integração e Angular.
- Fila, `current-state.yaml`, roadmap e evidências atualizados.

## Proibições respeitadas

Sem envio real, sem cobrança, sem crédito, sem merge em main, sem deploy.
